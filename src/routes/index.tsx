import React from 'react';
import { Switch } from 'react-router-dom';
import { RouteWithLayout } from '../components';

// Define routes here. Use the RouteWithLayout to your advantage
// if you want to use multiple layouts across files.
export const routes = (
  <Switch>

  </Switch>
);